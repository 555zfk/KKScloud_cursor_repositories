---
description: 约定 Cursor 生成文件的统一存放位置
alwaysApply: true
---

# 生成文件存放规范

所有由 Cursor/AI 生成的产物（报告、脚本、图片、PDF/Word/Excel、导出数据等），
统一放入工作区下的 `cursor生成文件/` 文件夹，保持工作区根目录整洁。

## 规则

- 不要把生成文件直接写到工作区根目录 `d:\WorkFiles\Cursor\`。
- 每个任务在 `cursor生成文件/` 下新建一个**描述性命名的子文件夹**，建议带日期：
  例如 `cursor生成文件/机器人结构材料选型分析_20260603/`。
- 同一任务的所有产物（含中间脚本、图片、缓存）都放进该子文件夹。
- 若 `cursor生成文件/` 不存在则先创建；子文件夹命名用「任务主题_YYYYMMDD」。

## 示例

```
cursor生成文件/
  机器人结构材料选型分析_20260603/
    机器人结构材料对比分析报告.pdf
    机器人结构材料对比分析报告.docx
    material_report.py
    radar_for_docx.png
```

## 例外

- 用户明确指定了其他输出路径时，遵循用户指定路径。
- 项目本身的源码/配置文件按其正常位置存放，不属于「生成产物」。
